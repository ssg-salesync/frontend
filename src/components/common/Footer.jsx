import { styled } from "styled-components";

const FooterDiv = styled.footer`
    height: 10vh;
    width: 100%;
`

function Footer() {
    return (
        <FooterDiv/>
    );
};

export default Footer